import { CommunitySubmissions } from '@/components/community-submissions';

export const metadata = { title: 'Your community submissions' };

export default function CommunityProposalSubmissionsPage() {
  return <CommunitySubmissions />;
}
