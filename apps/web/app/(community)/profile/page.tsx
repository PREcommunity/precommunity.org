import { SponsorProfileForm } from '@/components/sponsor-profile-form';

export const metadata = { title: 'Contribution profile' };

export default function ProfilePage() {
  return (
    <main>
      <SponsorProfileForm />
    </main>
  );
}
